# Global-Min-Cut-Calculator

Calculates the mininimum cut of a graph (need to input a file into Graph.java) using the probablistic method of making many random cuts until a minimum cut is probably found.
